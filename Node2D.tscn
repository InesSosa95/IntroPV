[gd_scene load_steps=5 format=2]

[ext_resource path="res://assets/white_square_64x64.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]

[sub_resource type="Shader" id=2]
code = "shader_type canvas_item;

uniform vec2 center = vec2(0.5,0.5);
uniform float radius = 0.4;
uniform float smoothing = 0.4;

float circle(vec2 position, float radio, vec2 centre) {
	float distance = distance(position, centre);
	return smoothstep(0.4,0.45, distance);
	
}

void fragment(){
	
	vec3 albedo = vec3(circle(UV, 1.0));
	
	COLOR = vec4(albedo,1.0);
}"

[sub_resource type="ShaderMaterial" id=3]
shader = SubResource( 2 )
script = SubResource( 1 )

[node name="Node2D" type="Node2D"]

[node name="Sprite" type="Sprite" parent="."]
material = SubResource( 3 )
texture = ExtResource( 1 )
